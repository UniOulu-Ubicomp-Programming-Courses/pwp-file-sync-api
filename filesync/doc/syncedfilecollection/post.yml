---
requestBody:
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/SyncedFile'
responses:
  '201':
    headers:
      Location:
        description: URI of the created resource
        schema:
          type: string
